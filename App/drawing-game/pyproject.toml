[project]
name = "drawing-game"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Tanner Andrews", email = "TannerAndrews859@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
drawing-game = "drawing_game:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
